<Window x:Class="RpaSuite.Presentation.Wpf.Views.DashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RPA Dashboard" Height="300" Width="500">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="RPA Suite" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>
            <Button Content="Executar Robô Manualmente" Command="{Binding ExecutarCommand}" Margin="0,0,0,8"/>
            <Button Content="Agendar com Hangfire" Command="{Binding AgendarCommand}" Margin="0,0,0,8"/>
            <TextBlock Text="{Binding Status}" FontSize="14" Margin="0,12,0,0"/>
        </StackPanel>
    </Grid>
</Window>
